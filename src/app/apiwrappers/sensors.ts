

// TOUCH API https://developer.mozilla.org/en-US/docs/Web/API/Touch_events

// https://intel.github.io/generic-sensor-demos/

// For now, my device doesn't support any of these so I can't use them.
// Fairly new API so not really standardized at all 

// // My laptop does NOT support this
// if(window.DeviceOrientationEvent){
//     window.addEventListener("deviceorientation", (e) => {
//         console.log(e.alpha, e.gamma, e.beta, e.absolute)
//         //console.log(e.timeStamp)
//     }, false);
// }

// // My laptop does not support this as well
// if(window.DeviceMotionEvent){
//     window.addEventListener("devicemotion", (e) => {
//         console.log(e.acceleration, e.accelerationIncludingGravity, e.rotationRate)
//         console.log(e.interval)
//     }, false);
// }